name: Deploy Menu Service

on:
  push:
    branches: [ main ]
    paths: [ 'menuService/**', 'k8s/production/menu-service/**' ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-south-1

      - name: Login to ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build & Push
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
        run: |
          docker build -t $ECR_REGISTRY/dineflow-menu:latest ./menuService
          docker push $ECR_REGISTRY/dineflow-menu:latest

      - name: Update KubeConfig
        run: aws eks update-kubeconfig --name dineflow-production-cluster --region ap-south-1

      - name: Deploy to EKS and Restart Pods
        run: |
          kubectl apply -f k8s/production/menu-service/menu-deployment.yaml -n dineflow-production
          
          kubectl rollout restart deployment menu-service -n dineflow-production
          kubectl rollout status deployment menu-service -n dineflow-production